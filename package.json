{
    "name": "metaschema-framework-website",
    "version": "1.0.0-rc.1",
    "devDependencies": {
        "@fullhuman/postcss-purgecss": "^7.0.2",
        "ajv-cli": "^5.0.x",
        "ajv-formats": "^3.0.x",
        "autoprefixer": "^10.4.23",
        "hugo-bin": "^0.149.2",
        "postcss": "^8.5.6",
        "postcss-cli": "^11.0.1",
        "sass": "^1.96.0",
        "yaml-convert": "^1.0.x"
    },
    "overrides": {
        "fast-json-patch": "^3.1.1"
    },
    "hugo-bin": {
        "buildTags": "extended",
        "version": "0.120.0"
    },
    "scripts": {
        "build": "cd website && hugo mod vendor && hugo -e production --minify",
        "serve": "cd website && hugo mod vendor && hugo server"
    }
}
